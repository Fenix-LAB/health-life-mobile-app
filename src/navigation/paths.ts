/*
Here we define the paths used in the application.
This allows us to easily reference paths throughout the app without hardcoding strings.

Example paths:

Example usage:
import { Paths } from './paths';
const examplePath = Paths.Example;
*/
export const enum Paths {
  Example = 'example',
  Startup = 'startup',
}
